/* Google Analytics bootstrap, kept external so the CSP can avoid
   'unsafe-inline'. gtag.js itself loads async from googletagmanager. */
window.dataLayer = window.dataLayer || [];
function gtag() {
  dataLayer.push(arguments);
}
gtag("js", new Date());
gtag("config", "G-6C4SD8CB3B");
